spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/customer?useSSL=false&serverTimezone=UTC
    username: amigoscode
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  server:
    port: 8080